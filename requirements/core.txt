# Core packages used by the Helix source snapshot. Full distributed execution
# additionally requires the matching patched DeepSpeed, Transformers, and THOP
# sources, which are not included in the initial public repository.
torch==2.5.1
tokenizers==0.20.3
safetensors==0.4.5
huggingface-hub==0.26.2
datasets==3.1.0
numpy==2.1.3
pydantic==2.9.2
hjson==3.1.0
ninja==1.11.1.1
psutil==6.1.0
packaging==24.2
tqdm==4.67.0
